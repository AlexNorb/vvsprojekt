---
import { Image } from "astro:assets";
interface Props {
  title: string;
  subtitle: string;
  image: string;
}

const { title, subtitle, image } = Astro.props;
---

<!-- SUB-PAGE HERO -->
<header
  class="relative h-[60vh] min-h-[500px] flex items-center justify-center bg-slate-900 overflow-hidden"
>
  <div class="absolute inset-0 z-0">
    <Image
      src={image}
      alt={title}
      class="w-full h-full object-cover opacity-20"
      width={1920}
      height={1080}
      inferSize={true}
    />
    <div
      class="absolute inset-0 bg-gradient-to-r from-slate-900 via-slate-900/80 to-transparent"
    >
    </div>
  </div>

  <div class="relative z-10 max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 w-full">
    <div class="max-w-3xl">
      <h1
        class="text-4xl md:text-6xl font-display font-bold text-white mb-6 leading-tight animate-fade-in-up"
        set:html={title}
      />
      <p
        class="text-xl text-slate-300 leading-relaxed max-w-2xl animate-fade-in-up border-l-4 border-elorange pl-6"
        style="animation-delay: 0.1s;"
      >
        {subtitle}
      </p>
    </div>
  </div>
</header>
